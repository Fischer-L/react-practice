schema {
  query: Query
  mutation: Mutation
}

type Query {
  hello(name: String!): String!
  menu: [MenuItem!]!
}

type Mutation {
  wink(name: String!): String!
}

type MenuItem {
  name: String!,
  price: Float!,
}

